import Layout from '@/common/Layout';
import CartPage from '@/modules/CartPage';

const Cart = () => {
  return (
    <Layout pageCategory="CartPage">
      <CartPage />
    </Layout>
  );
};

export default Cart;
